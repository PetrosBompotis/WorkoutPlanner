<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorkoutPlanner"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".ShareWorkoutActivity"
            android:exported="false" />
        <activity
            android:name=".UserSharedPostsActivity"
            android:exported="false" />
        <activity
            android:name=".postDetailActivity.PostDetailActivity"
            android:exported="false" />
        <activity
            android:name=".exerciseDetailActivity.ExerciseDetailActivity"
            android:exported="false" />
        <activity
            android:name=".exerciseActivity.ExerciseActivity"
            android:exported="false" />
        <activity
            android:name=".adminActivity.AdminActivity"
            android:exported="false" />
        <activity
            android:name=".userActivity.UserActivity"
            android:exported="false" />
        <activity
            android:name=".mainActivity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>